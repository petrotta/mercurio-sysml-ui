[package]
name = "mercurio-model"
version = "0.1.0"
edition = "2021"

[dependencies]
salsa = "0.18"
serde = { version = "1", features = ["derive"] }
syster = { package = "syster-base", version = "0.3.5-alpha" }
